import React from 'react';
import { MainContainer, OurWorks, SectionContainer } from '../components';

const Home = () => {
	return (
		<div>
			<MainContainer />
			<SectionContainer />
			<OurWorks />
		</div>
	);
};

export default Home;
